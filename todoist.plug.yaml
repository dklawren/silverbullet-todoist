name: Todoist
requiredPermissions:
  - fetch
functions:
  getTasks:
    path: todoist.ts:getTasks
    events:
      - query:todoist
  # addTask:
  #   path: todoist.ts:addTask
  #   command:
  #     name: "Todoist: Add Task"
